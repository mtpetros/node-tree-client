# node-tree-client
Node Tree Client

Started as part of [node-tree](https://github.com/mtpetros/node-tree) repository, which was then split into a separate repo (and polished some more) to satisfy some deployment requirements in Heroku.
